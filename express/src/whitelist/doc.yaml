paths:
  /whitelist:
    get:
      responses:
        "200":
          description: Successful operation
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: array
              items:
                type: object
                properties:
                  name:
                    type: string
                    example: harits
                    required: true
      responses:
        "200":
          description: Successful operation
